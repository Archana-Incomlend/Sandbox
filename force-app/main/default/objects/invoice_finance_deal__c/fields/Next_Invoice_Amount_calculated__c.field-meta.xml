<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Next_Invoice_Amount_calculated__c</fullName>
    <externalId>false</externalId>
    <formula>IF(Credit_Limit_Incomlend_for_this_Map__c =0,0,
IF(ISBLAnk(Next_Invoice_Amount__c),invoice_amount__c,
IF(Next_Invoice_Amount__c=0, 0,
Next_Invoice_Amount__c )))</formula>
    <inlineHelpText>Calculated, if Invoice in collection the value will be 0 by default</inlineHelpText>
    <label>Next Invoice Amount (calculated)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
