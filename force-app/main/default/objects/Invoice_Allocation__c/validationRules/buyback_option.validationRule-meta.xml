<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>buyback_option</fullName>
    <active>true</active>
    <errorConditionFormula>AND(NOT(isBlank(Buyback_Enter_Date__c)), NOT(isBlank(Buybacked_Date__c)))</errorConditionFormula>
    <errorDisplayField>Buyback_Enter_Date__c</errorDisplayField>
    <errorMessage>Can only input either this or Buyback Close Date and not both</errorMessage>
</ValidationRule>
